export const createRemoteJWKSet = () => () => {
  throw new Error('jose is stubbed in e2e — TestAuthGuard should have bypassed this');
};
export const jwtVerify = async () => {
  throw new Error('jose is stubbed in e2e — TestAuthGuard should have bypassed this');
};
