import { Module } from '@nestjs/common';

// TODO: Implement ExtensionsController and ExtensionsService
// Endpoints to implement:
//   POST  /extensions                — request deadline extension
//   GET   /extensions                — list pending extensions for org (MANAGER+)
//   PATCH /extensions/:id/resolve    — approve/reject extension (MANAGER+)

@Module({})
export class ExtensionsModule {}
