import { Module } from '@nestjs/common';

// TODO: Implement MeetingNotesController and MeetingNotesService
// Endpoints to implement:
//   POST   /meeting-notes              — create note
//   GET    /meeting-notes              — list notes for org
//   GET    /meeting-notes/:id          — get note with extracted items
//   PATCH  /meeting-notes/:id          — update note
//   DELETE /meeting-notes/:id          — soft delete note
//   POST   /meeting-notes/:id/extract  — trigger AI extraction

@Module({})
export class MeetingNotesModule {}
