// TODO: Create ticket form
// - Title, description, priority, assignee, due date fields
// - Validates with createTicketSchema from @ticketbot/shared-validation
// - Submits to POST /api/v1/tickets
export default function NewTicketPage() {
  return <h1>Create Ticket — TODO</h1>;
}
