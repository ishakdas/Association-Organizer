# Bot runs inside the API — these vars are set in the API's .env
# Listed here for reference only.

BOT_TOKEN="your-telegram-bot-token"
API_URL="http://localhost:3000"
WEB_URL="http://localhost:3001"
